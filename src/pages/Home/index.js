import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Home = () => {
  return (
    <View>
      <Text>Splash</Text>
    </View>
  );
};

export default Home;

const styles = StyleSheet.create({});
